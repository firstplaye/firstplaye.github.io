<% if (site.posts.length){ %>
  <div class="widget">
    <h3 class="widget-title"><%= __('recent_posts') %></h3>
    <ul>
      <%- list_posts({ amount: theme.recent_posts_limits || 5 }) %>
    </ul>
  </div>
<% } %>
